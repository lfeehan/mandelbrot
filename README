mandelbrot fractal & postscript exercise.

Usage: java mandel.java < input.txt > image.ps

	pipe standard input output to filename.ps
	generates image.ps using the input from input.txt
	
	input.txt:
	1. n (degree of set)
	2. minimum x value
	3. minimum y value
	4. max x value
	5. max y value
	6+. 256 lines of rgb color values in range 0 to 1.


